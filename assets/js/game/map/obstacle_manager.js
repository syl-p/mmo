import ResourceManager from "../resource_manager";

export default class ObstacleManager {

	/**
	 * @param {ResourceManager} resourceManager
	 */
	constructor(resourceManager) {
		console.log(resourceManager.items)
	}
}
